{
  "name": "ralph-complete",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "bunx wrangler dev",
    "deploy": "bunx wrangler deploy",
    "build": "docker build -t ralph-opencode .",
    "push": "bunx wrangler containers push ralph-opencode",
    "test:prod": "playwright test test-prod.ts"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260111.0",
    "@playwright/test": "^1.57.0",
    "wrangler": "4"
  },
  "dependencies": {
    "@cloudflare/containers": "^0.0.31"
  }
}
